import { styled } from "@stitches/react";

export const Wrapper = styled("div", {
  display: "flex",
  gap: "2rem",
  margin: "3rem 0 0",
});
export const WrapButtons = styled("div", {
  display: "flex",
});
